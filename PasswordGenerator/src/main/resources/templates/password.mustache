<!doctype html>
<html lang="ko">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width,initial-scale=1"/>
    <title>{{title}}</title>
    <style>
        body{font-family:system-ui,-apple-system,Segoe UI,Roboto,sans-serif;margin:0;background:#0b0c10;color:#eaeef2}
        .wrap{max-width:720px;margin:48px auto;padding:0 16px}
        .card{background:#11151a;border:1px solid #2a3441;border-radius:14px;padding:20px}
        .row{display:flex;gap:12px;align-items:center;margin:12px 0;flex-wrap:wrap}
        label{font-size:14px;color:#c8d2df}
        input[type=number]{width:90px;padding:8px;border-radius:8px;border:1px solid #334151;background:#0f1318;color:#eaeef2}
        button{padding:10px 16px;border-radius:10px;border:1px solid #3b6aff;background:#3b6aff;color:#fff;font-weight:600;cursor:pointer}
        .output{margin-top:12px;padding:14px;min-height:44px;border-radius:10px;border:1px dashed #334151;background:#0c1015;word-break:break-all}
        .error{color:#ff7b7b;margin-top:8px}
        .hint{color:#98a6b8;font-size:12px;margin-top:10px}
    </style>
</head>
<body>
<main class="wrap">
    <h1>{{title}}</h1>
    <section class="card">
        <form method="post" action="/password">
            <div class="row">
                <label for="length">길이</label>
                <input id="length" name="length" type="number" min="4" max="64" value="{{req.length}}">
            </div>
            <div class="row">
                <label><input type="checkbox" name="lower"  {{#req.lower}}checked{{/req.lower}}> 소문자</label>
                <label><input type="checkbox" name="upper"  {{#req.upper}}checked{{/req.upper}}> 대문자</label>
                <label><input type="checkbox" name="number" {{#req.number}}checked{{/req.number}}> 숫자</label>
                <label><input type="checkbox" name="symbol" {{#req.symbol}}checked{{/req.symbol}}> 기호</label>
            </div>
            <div class="row">
                <button type="submit">서버에서 생성</button>
            </div>
        </form>

        {{#result}}
            <div class="output" title="생성된 비밀번호">
                {{result}}
            </div>
        {{/result}}

        {{#error}}
            <div class="error">{{error}}</div>
        {{/error}}

        <p class="hint">TIP: 길이를 늘리고 문자 종류를 다양하게 선택하세요.</p>
    </section>
</main>
</body>
</html>
